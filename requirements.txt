numpy
scipy
matplotlib
certifi
PyYAML        # configs/*.yaml; main.py falls back to a tiny parser without it
cgal          # official CGAL Python bindings - fallback reference only, the compiled
              # bin/cgal_delaunay is preferred (script/build_tools.sh)

# Not on PyPI, installed from source by script/first_install.sh:
#   torch                 CUDA build matching the cluster's driver
#   paragram              patched by script/patch_paragram.py
#   gdel3d (pyGDel3D)     patched by script/patch_pygdel3d.py
#   geodel                pip install "geodel @ git+https://github.com/Anttwo/GeoDel@v0.1.0"
